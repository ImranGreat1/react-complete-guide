    /* Doing the same using arrays but we'll to provide a unique key
    for each element and seperate them with comma
    */

const AuxillaryChild = () => {
    return [
        <h3 key="djdnkm">Using Arrays to render adjacent JSX</h3>,
        <p key="hqdme">This is the reason returning arrays of components using map works</p>
    ]
}


export default AuxillaryChild;